# AeroSpace Configuration - Hyprland Style
# This config matches typical Hyprland keybindings and conventions for consistency
# between macOS (AeroSpace) and Linux (Hyprland) systems.
#
# Key changes from default AeroSpace:
# - Uses Cmd (Super) key instead of Alt as primary modifier
# - Includes both hjkl and arrow key navigation
# - Cmd+T for iTerm2 terminal (Hyprland standard)
# - Cmd+Q for close window, Cmd+F for fullscreen
# - Cmd+Shift+Space for floating toggle
# - Cmd+Backslash for split direction toggle
# - Service mode accessible via Cmd+Shift+E
# - Slightly larger gaps (12px) typical of Hyprland configs
# - Workspaces 1-10 instead of 1-9
#
# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login - POPULAR CHANGE
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*) - HYPRLAND STYLE
# Slightly larger gaps to match typical Hyprland configs
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = 10
    inner.vertical =   10
    outer.left =       10
    outer.bottom =     10
    outer.top =        10
    outer.right =      10

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # iTerm2 shortcut - HYPRLAND STYLE (Super/Cmd + T)
    # cmd-t = '''exec-and-forget osascript -e '
    # tell application "iTerm"
    #     create window with default profile
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    # Layout toggles - HYPRLAND STYLE
    cmd-shift-space = 'layout floating tiling'  # Toggle floating
    cmd-backslash = 'layout tiles horizontal vertical'  # Toggle split direction
    cmd-shift-backslash = 'layout accordion horizontal vertical'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    # Movement - HYPRLAND STYLE (hjkl navigation)
    cmd-shift-alt-ctrl-h = 'focus left'
    cmd-shift-alt-ctrl-j = 'focus down'
    cmd-shift-alt-ctrl-k = 'focus up'
    cmd-shift-alt-ctrl-l = 'focus right'
    
    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    # Move windows - HYPRLAND STYLE
    cmd-shift-h = 'move left'
    cmd-shift-j = 'move down'
    cmd-shift-k = 'move up'
    cmd-shift-l = 'move right'
    
    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    # Resize - HYPRLAND STYLE
    cmd-alt-h = 'resize width -25'
    cmd-alt-j = 'resize height -25'
    cmd-alt-k = 'resize width +25'
    cmd-alt-l = 'resize height +25'

    # See: https://nikitabobko.github.io/AeroSpace/commands#balance-sizes
    # Resize - HYPRLAND STYLE
    cmd-alt-o = 'balance sizes'


    # # Core window management - HYPRLAND STYLE
    cmd-q = 'close'                    # Close window
    # cmd-f = 'fullscreen'               # Fullscreen toggle
    # 
    # # Monitor navigation - HYPRLAND STYLE
    # cmd-period = 'focus-monitor --wrap-around next'
    # cmd-shift-period = 'move-node-to-monitor --wrap-around next'
    #
    # # Workspaces - HYPRLAND STYLE (Super + number)
    cmd-shift-alt-ctrl-1 = 'workspace 1'
    cmd-shift-alt-ctrl-2 = 'workspace 2'
    cmd-shift-alt-ctrl-3 = 'workspace 3'
    cmd-shift-alt-ctrl-4 = 'workspace 4'
    cmd-shift-alt-ctrl-5 = 'workspace 5'
    cmd-shift-alt-ctrl-6 = 'workspace 6'
    cmd-shift-alt-ctrl-7 = 'workspace 7'
    cmd-shift-alt-ctrl-8 = 'workspace 8'
    cmd-shift-alt-ctrl-9 = 'workspace 9'
    cmd-shift-alt-ctrl-0 = 'workspace 10'

    # # Move windows to workspaces - HYPRLAND STYLE
    ctrl-alt-1 = 'move-node-to-workspace 1'
    ctrl-alt-2 = 'move-node-to-workspace 2'
    ctrl-alt-3 = 'move-node-to-workspace 3'
    ctrl-alt-4 = 'move-node-to-workspace 4'
    ctrl-alt-5 = 'move-node-to-workspace 5'
    ctrl-alt-6 = 'move-node-to-workspace 6'
    ctrl-alt-7 = 'move-node-to-workspace 7'
    ctrl-alt-8 = 'move-node-to-workspace 8'
    ctrl-alt-9 = 'move-node-to-workspace 9'
    ctrl-alt-0 = 'move-node-to-workspace 10'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    cmd-tab = 'workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    # cmd-shift-tab = 'move-workspace-to-monitor --wrap-around next'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    # Service mode - HYPRLAND STYLE (Super + Shift + E for "exit/reload")
    # cmd-shift-e = 'mode service'

# 'service' binding mode declaration - HYPRLAND STYLE
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# [mode.service.binding]
#     esc = ['reload-config', 'mode main']
#     r = ['flatten-workspace-tree', 'mode main'] # reset layout
#     f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
#     backspace = ['close-all-windows-but-current', 'mode main']
#
#     # Exit AeroSpace - HYPRLAND STYLE
#     q = 'close-all-windows-but-current'
#     cmd-shift-e = 'mode main'  # Exit service mode
#
#     # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#     #s = ['layout sticky tiling', 'mode main']
#
#     # Join containers - HYPRLAND STYLE (using hjkl)
#     h = ['join-with left', 'mode main']
#     j = ['join-with down', 'mode main'] 
#     k = ['join-with up', 'mode main']
#     l = ['join-with right', 'mode main']
#
#     # Volume controls (bonus Hyprland-style media keys)
#     down = 'volume down'
#     up = 'volume up'
#     shift-down = ['volume set 0', 'mode main']

# App-specific rules - HYPRLAND STYLE
# Some apps work better as floating windows
# [[on-window-detected]]
# if.app-id = 'com.apple.finder'
# run = 'layout floating'
#
# [[on-window-detected]]
# if.app-id = 'com.apple.systempreferences'
# run = 'layout floating'
#
# [[on-window-detected]]
# if.app-id = 'com.apple.ActivityMonitor'
# run = 'layout floating'
#
# [[on-window-detected]]
# if.app-id = 'com.apple.calculator'
# run = 'layout floating'
#
# [[on-window-detected]]
# if.app-id = 'com.apple.archiveutility'
# run = 'layout floating'

# Hyprland-style workspace assignments (COMMENTED OUT - Manual control preferred)
# Workspace 1: Web browsers
# [[on-window-detected]]
# if.app-id = 'com.apple.Safari'
# run = 'move-node-to-workspace 1'

# [[on-window-detected]]
# if.app-id = 'com.google.Chrome'
# run = 'move-node-to-workspace 1'

# Workspace 2: Code editors  
# [[on-window-detected]]
# if.app-id = 'com.microsoft.VSCode'
# run = 'move-node-to-workspace 2'

# Workspace 3: Terminals (matching typical Hyprland setup)
# [[on-window-detected]]
# if.app-id = 'com.googlecode.iterm2'
# run = 'move-node-to-workspace 3'

